{
  "name": "subscription_reconciler",
  "version": "1.0.0",
  "description": "Subscription Reconciler — Adora Backend Engineer Assignment",
  "main": "dist/index.js",
  "scripts": {
    "dev": "nodemon --watch src --ext ts,sql --exec ts-node src/index.ts",
    "build": "tsc && npm run copy-assets",
    "copy-assets": "mkdir -p dist/db && cp src/db/schema.sql dist/db/schema.sql",
    "start": "node dist/index.js",
    "test": "echo \"No tests yet\""
  },
  "keywords": [],
  "author": "MuGi",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "node-cron": "^4.2.1",
    "pg": "^8.21.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^25.9.1",
    "@types/pg": "^8.20.0",
    "nodemon": "^3.1.14",
    "ts-node": "^10.9.2",
    "typescript": "^6.0.3"
  }
}
